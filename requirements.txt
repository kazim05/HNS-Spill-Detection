# HNS-Spill-Detection
# Python 3.6 / 3.7 — the code is TensorFlow 1.x graph mode (tf.placeholder,
# tf.Session, tf.variable_scope) and will NOT run on TensorFlow 2.x.

# --- core ---
tensorflow-gpu==1.11.0   # CPU only? use: tensorflow==1.11.0
numpy==1.15.1
easydict                 # core/config.py
opencv-python            # cv2 — dataset / utils / evaluate / image_demo
Pillow==5.3.0            # image_demo.py
tqdm                     # train.py progress bar

# --- mAP/main.py ---
matplotlib               # precision-recall & mAP plots

# --- listed in the original requirements file, not imported by the code ---
scipy==1.1.0
seaborn==0.9.0
wget==3.2
